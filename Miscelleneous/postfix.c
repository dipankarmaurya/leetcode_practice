#include<stdio.h>

int main(){
	char stack[100],top=-1;
	char p[100];
	char q[100];
	scanf("%s",q);
	int length= strlen(q);
	int i,j;
	stack[++top]='(';
	q[len]=')';
	for(i=0,;i<len+1;i++,)
	{
		if(q[i]>='a'||q[i]<='z'){
		   for(k=0;k<len;k++)
		     p[k]=q[i] ;
	    }
	    if(q[i]=='(';
	    stack[++top]=q[i];
	    if(q[i]=='+'||q[i]=='-'||q[i]=='*'||q[i]=='/')
	    
		
	}
}
