#include<stdio.h>
#include<string.h>
int main()
{
    int n,i,j,max;
    char s[1000];
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%s",s);
        max=s;
    }
    
        
    
    for(i=0;i<n;i++)
    printf("%s",s);
}

