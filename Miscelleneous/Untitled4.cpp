#include<bits/stdc++.h>
using namespace std;

int main()
{
int t;
int  m;
int n;
string ss;
string aa;
vector<string>s;
vector<string> a;

cin>>t;
while(t--){
cin>>ss;
s.push_back(ss);
cin>>aa;
a.push_back(aa);
}
int count =0;
std::vector<int>::iterator p;
while(next_permutation(s.begin(),s.end() )){
   auto p =std::find(s.begin(),s.end(),a)){
	if(auto p=s.end){
		count+=0;
	}
	else
	coun+=1;
}
	cout<<count;
}
}
